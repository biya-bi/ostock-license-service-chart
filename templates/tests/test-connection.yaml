apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "licensing-service.fullname" . }}-test-connection"
  labels:
    {{- include "licensing-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "licensing-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
